namespace Galaxy.Core.Tests
{
    public class TestSettings
    {
        public const string UnitPattern = "^([a-z\\s]+) is ([IVXLCDM])$";

        public const string ResourcePattern = "^([a-z\\s]+) ([A-Z][a-z]+) is (\\d+) Credits$";

        public const string UnitQuestionPattern = "^how much is ([a-z\\s]+)\\?$";

        public const string ResourceQuestionPattern = "^how many Credits is ([a-z\\s]+) ([A-Z][a-z]+) \\?$";
    }
}